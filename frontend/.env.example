# URL pública del sitio (para meta tags y JSON-LD). Opcional.
# NEXT_PUBLIC_SITE_URL=https://tu-dominio.vercel.app

# Si la API de búsqueda corre aparte (ej. node api/server.js en otro puerto):
# NEXT_PUBLIC_API_URL=http://localhost:4000
